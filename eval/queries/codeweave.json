{
  "repo": "codeweave",
  "note": "This-repo-only retrieval fixtures. Each query lists the file path(s) a good ranker should surface in the top results. Targets are workspace-relative paths; a result counts as a hit when its path matches an expected target. Keep this set small and real so the v1 baseline is trustworthy and P4 can be gated against it.",
  "queries": [
    { "id": "sym-resolve-access", "category": "exact-symbol", "query": "resolve_access tool profile", "expected": ["src/tools/mod.rs"] },
    { "id": "sym-workspace-manager", "category": "exact-symbol", "query": "WorkspaceManager initialize actor", "expected": ["src/manager.rs"] },
    { "id": "sym-bash-supervisor", "category": "exact-symbol", "query": "BashSupervisor run background", "expected": ["src/bash.rs"] },
    { "id": "file-config-example", "category": "filename-lookup", "query": "config example json", "expected": ["config.example.json"] },
    { "id": "file-cargo", "category": "filename-lookup", "query": "Cargo manifest dependencies", "expected": ["Cargo.toml"] },
    { "id": "intent-tool-profiles", "category": "nl-intent", "query": "how are tool profiles selected and which tools each profile exposes", "expected": ["src/tools/mod.rs"] },
    { "id": "intent-token-auth", "category": "nl-intent", "query": "where is the bearer token generated and validated", "expected": ["src/main.rs"] },
    { "id": "intent-index-cache", "category": "nl-intent", "query": "code index cache schema and cache hit detection", "expected": ["src/index/mod.rs"] },
    { "id": "disc-context-ranking", "category": "impl-discovery", "query": "context retrieval scoring and result ranking", "expected": ["src/index/mod.rs"] },
    { "id": "disc-code-transaction", "category": "impl-discovery", "query": "apply multi file edit transaction rollback validation", "expected": ["src/workspace/edit.rs", "src/workspace/mod.rs"] },
    { "id": "disc-git-push", "category": "impl-discovery", "query": "git push confirm remote branch", "expected": ["src/repository.rs"] },
    { "id": "disc-symbol-extract", "category": "impl-discovery", "query": "extract symbols from source with tree sitter", "expected": ["src/symbols.rs"] },
    { "id": "disc-http-transport", "category": "impl-discovery", "query": "streamable http server session manager mcp", "expected": ["src/mcp_transport.rs"] },
    { "id": "disc-path-security", "category": "impl-discovery", "query": "validate relative path canonical root traversal", "expected": ["src/security.rs"] },
    { "id": "tests-workspace", "category": "tests-for-symbol", "query": "workspace edit transaction tests", "expected": ["src/workspace/tests.rs"] }
  ]
}
